<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{{ page_title }}</title>
    <style>
        body {
            font-family: DejaVu Sans, sans-serif;
            font-size: 10px;
        }
        h1 {
            font-size: 16px;
            margin-bottom: 4px;
        }
        .meta {
            font-size: 9px;
            color: #555;
            margin-bottom: 8px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 8px;
        }
        th, td {
            border: 1px solid #ccc;
            padding: 3px 4px;
        }
        th {
            background-color: #f2f2f2;
            font-weight: bold;
        }
        .text-right {
            text-align: right;
        }
        .small {
            font-size: 9px;
        }
        .summary {
            margin-top: 6px;
            margin-bottom: 6px;
            font-size: 9px;
        }
    </style>
</head>
<body>

<h1>{{ page_title }}</h1>
<div class="meta">
    {{ period_label }} – Généré le {{ now|date('d/m/Y H:i') }}
</div>

<div class="summary">
    <strong>Nombre de lignes :</strong> {{ total_lines }}<br>
    <strong>Quantité totale vendue :</strong> {{ total_quantity }}<br>
    <strong>Chiffre d'affaires estimé :</strong>
    {{ total_amount|number_format(2, ',', ' ') }} €
</div>

<table>
    <thead>
        <tr>
            <th>Date</th>
            <th>Magasin</th>
            <th>Vendeuse</th>
            <th>Référence</th>
            <th>Produit</th>
            <th>Taille</th>
            <th class="text-right">Qté</th>
            <th class="text-right">PU (€)</th>
            <th class="text-right">Total (€)</th>
        </tr>
    </thead>
    <tbody>
    {% if movements is empty %}
        <tr>
            <td colspan="9" class="small">
                Aucune vente pour cette période / ces filtres.
            </td>
        </tr>
    {% else %}
        {% for movement in movements %}
            {% set product = movement.product %}
            {% set qty = movement.quantity ?? 0 %}
            {% set unitPrice = (product and product.price is not null) ? product.price : 0 %}
            {% set lineTotal = unitPrice * qty %}
            <tr>
                <td class="small">
                    {{ movement.createdAt ? movement.createdAt|date('d/m/Y H:i') : '-' }}
                </td>
                <td class="small">
                    {% if movement.location %}
                        {{ movement.location.code }}
                    {% else %}
                        -
                    {% endif %}
                </td>
                <td class="small">
                    {% if movement.user %}
                        {{ movement.user.username }}
                    {% else %}
                        -
                    {% endif %}
                </td>
                <td class="small">
                    {% if product %}
                        {{ product.reference }}
                    {% else %}
                        -
                    {% endif %}
                </td>
                <td class="small">
                    {% if product %}
                        {{ product.name }}
                    {% else %}
                        -
                    {% endif %}
                </td>
                <td class="small">
                    {% if movement.size %}
                        {{ movement.size.name }}
                    {% else %}
                        -
                    {% endif %}
                </td>
                <td class="text-right small">
                    {{ qty }}
                </td>
                <td class="text-right small">
                    {% if unitPrice %}
                        {{ unitPrice|number_format(2, ',', ' ') }}
                    {% else %}
                        -
                    {% endif %}
                </td>
                <td class="text-right small">
                    {% if lineTotal %}
                        {{ lineTotal|number_format(2, ',', ' ') }}
                    {% else %}
                        -
                    {% endif %}
                </td>
            </tr>
        {% endfor %}
    {% endif %}
    </tbody>
</table>

</body>
</html>
