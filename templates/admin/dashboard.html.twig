{% extends 'base.html.twig' %}

{% block title %}{{ page_title ?? 'Admin' }}{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-3">{{ page_title }}</h1>

        <p class="text-muted">
            Bienvenue dans l’interface administrateur.
            On viendra mettre ici les raccourcis vers : produits, réassort, journal, etc.
        </p>

        <a href="{{ path('app_logout') }}" class="btn btn-outline-danger btn-sm mt-3">
            Se déconnecter
        </a>
    </div>
{% endblock %}
