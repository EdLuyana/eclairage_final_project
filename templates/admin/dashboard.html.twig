{% extends 'base.html.twig' %}

{% block title %}{{ page_title ?? 'Tableau de bord admin' }}{% endblock %}

{% block body %}
<div class="container py-4">

    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="h3 mb-1">Tableau de bord administrateur</h1>
            <p class="text-muted mb-0 small">
                Accédez aux principales fonctionnalités de gestion du catalogue, du stock et de l’activité.
            </p>
        </div>
        {# Si tu veux un lien vers le panneau vendeuse : #}
        <a href="{{ path('user_dashboard') }}" class="btn btn-outline-secondary btn-sm">← Panneau vendeuse</a>
         <a href="{{ path('app_logout') }}" class="btn btn-outline-danger btn-sm">
                Déconnexion
            </a>
    </div>

    {# LIGNE 1 : les plus utilisées #}
    <div class="row g-3 mb-4">

        {# (1) Création de produit et éléments liés -> /admin/create (admin_create_index) #}
        <div class="col-6 col-md-3">
            <a href="{{ path('admin_create_index') }}" class="text-decoration-none text-reset">
                <div class="card h-100 shadow-sm border-0 tile-hover">
                    <div class="card-body d-flex flex-column align-items-center justify-content-center text-center py-3">
                        <div class="fs-1 mb-2">✨</div>
                        <div class="fw-semibold text-center">Nouveau produit</div>
                        <div class="text-muted small mt-1 text-center">
                            Créer un produit et ses éléments liés (marque, catégorie, saison…).
                        </div>
                    </div>
                </div>
            </a>
        </div>

        {# (2) Stock -> /admin/stock (admin_stock_index) #}
        <div class="col-6 col-md-3">
            <a href="{{ path('admin_stock_index') }}" class="text-decoration-none text-reset">
                <div class="card h-100 shadow-sm border-0 tile-hover">
                    <div class="card-body d-flex flex-column align-items-center justify-content-center text-center py-3">
                        <div class="fs-1 mb-2">📦</div>
                        <div class="fw-semibold">Stock global</div>
                        <div class="text-muted small mt-1">
                            Consulter les stocks par magasin, taille et couleur.
                        </div>
                    </div>
                </div>
            </a>
        </div>

        {# (3) Journal des ventes -> /admin/sales (admin_sales_index) #}
        <div class="col-6 col-md-3">
            <a href="{{ path('admin_sales_index') }}" class="text-decoration-none text-reset">
                <div class="card h-100 shadow-sm border-0 tile-hover">
                    <div class="card-body d-flex flex-column align-items-center justify-content-center text-center py-3">
                        <div class="fs-1 mb-2">💶</div>
                        <div class="fw-semibold">Journal des ventes</div>
                        <div class="text-muted small mt-1">
                            Suivi du chiffre d’affaires par période, magasin et vendeuse.
                        </div>
                    </div>
                </div>
            </a>
        </div>

        {# (5) Tous les produits enregistrés -> /admin/products (admin_product_index) #}
        <div class="col-6 col-md-3">
            <a href="{{ path('admin_product_index') }}" class="text-decoration-none text-reset">
                <div class="card h-100 shadow-sm border-0 tile-hover">
                    <div class="card-body d-flex flex-column align-items-center justify-content-center text-center py-3">
                        <div class="fs-1 mb-2">🗂️</div>
                        <div class="fw-semibold">Tous les produits</div>
                        <div class="text-muted small mt-1">
                            Liste complète des produits enregistrés, filtres et actions.
                        </div>
                    </div>
                </div>
            </a>
        </div>
    </div>

    {# LIGNE 2 : tuiles présentes mais non encore implémentées #}
    <div class="row g-3 mb-4">

        {# (4) Réassort -> admin_reassort_index #}
<div class="col-6 col-md-3">
    <a href="{{ path('admin_reassort_index') }}" class="text-decoration-none text-reset">
        <div class="card h-100 shadow-sm border-0 tile-hover">
            <div class="card-body d-flex flex-column align-items-center justify-content-center text-center py-3">
                <div class="fs-1 mb-2">🔁</div>
                <div class="fw-semibold">Réassort</div>
                <div class="text-muted small mt-1">
                    Préparez un lot de produits et générez toutes les étiquettes en une fois.
                </div>
            </div>
        </div>
    </a>
</div>

   {# (6) Journal d'activité -> admin_activity_index #}
<div class="col-6 col-md-3">
    <a href="{{ path('admin_activity_index') }}" class="text-decoration-none text-reset">
        <div class="card h-100 shadow-sm border-0 tile-hover">
            <div class="card-body d-flex flex-column align-items-center justify-content-center text-center py-3">
                <div class="fs-1 mb-2">🕒</div>
                <div class="fw-semibold">Journal d’activité</div>
                <div class="text-muted small mt-1">
                    Mouvements hors ventes : ajouts, retours, ajustements, transferts…
                </div>
            </div>
        </div>
    </a>
</div>

{# (7) Configuration des promotions -> admin_promotion_index #}
<div class="col-6 col-md-3">
    <a href="{{ path('admin_discounts_index') }}" class="text-decoration-none text-reset">
        <div class="card h-100 shadow-sm border-0 tile-hover">
            <div class="card-body d-flex flex-column align-items-center justify-content-center text-center py-3">
                <div class="fs-1 mb-2">💸</div>
                <div class="fw-semibold">Promotions & soldes</div>
                <div class="text-muted small mt-1">
                    Créer et activer les remises applicables par les vendeuses.
                </div>
            </div>
        </div>
    </a>
</div>

       {# (8) Gestion des comptes vendeuse et admin -> admin_user_index #}
<div class="col-6 col-md-3">
    <a href="{{ path('admin_user_index') }}" class="text-decoration-none text-reset">
        <div class="card h-100 shadow-sm border-0 tile-hover">
            <div class="card-body d-flex flex-column align-items-center justify-content-center text-center py-3">
                <div class="fs-1 mb-2">👥</div>
                <div class="fw-semibold">Comptes utilisateurs</div>
                <div class="text-muted small mt-1">
                    Gérer les comptes vendeuses et administrateurs.
                </div>
            </div>
        </div>
    </a>
</div>

    </div>

</div>

<style>
    .tile-hover {
        transition: transform 0.12s ease, box-shadow 0.12s ease;
    }
    .tile-hover:hover {
        transform: translateY(-2px);
        box-shadow: 0 0.5rem 1rem rgba(0,0,0,0.12);
    }

    .tile-disabled {
        opacity: 0.6;
    }
    .disabled-tile {
        cursor: not-allowed;
        pointer-events: none;
    }
</style>
{% endblock %}
